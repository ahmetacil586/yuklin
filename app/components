export default function Header() {
  return (
    <header className="bg-white border-b shadow-sm sticky top-0 z-50">
      <div className="max-w-7xl mx-auto flex items-center justify-between px-6 py-4">

        <div className="flex items-center gap-3">
          <span className="text-3xl">🚛</span>

          <div>
            <h1 className="text-2xl font-extrabold text-blue-700">
              YÜKLİN
            </h1>

            <p className="text-xs text-gray-500">
              Yükünü Paylaş, Teklifleri Karşılaştır, Güvenle Taşıt.
            </p>
          </div>
        </div>

        <nav className="hidden lg:flex gap-8 font-medium text-gray-700">
          <a href="#" className="hover:text-blue-600">Ana Sayfa</a>
          <a href="#" className="hover:text-blue-600">Yük İlanı</a>
          <a href="#" className="hover:text-blue-600">Boş Araç</a>
          <a href="#" className="hover:text-blue-600">Hakkımızda</a>
          <a href="#" className="hover:text-blue-600">İletişim</a>
        </nav>

        <div className="flex gap-3">
          <button className="border border-blue-600 text-blue-600 px-5 py-2 rounded-xl font-semibold hover:bg-blue-50">
            Giriş Yap
          </button>

          <button className="bg-blue-600 text-white px-5 py-2 rounded-xl font-semibold hover:bg-blue-700">
            Kayıt Ol
          </button>
        </div>

      </div>
    </header>
  );
}